<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jiin.backend.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.jiin.backend.domain.User">
        <id     property="userId"        column="user_id"/>
        <result property="provider"      column="provider"/>
        <result property="providerId"    column="provider_id"/>
        <result property="providerEmail" column="provider_email"/>
        <result property="nickname"      column="nickname"/>
        <result property="profileImage"  column="profile_image"/>
        <result property="isActive"      column="is_active"/>
        <result property="createdAt"     column="created_at"/>
        <result property="updatedAt"     column="updated_at"/>
    </resultMap>

    <select id="findByProviderAndProviderId" statementType="CALLABLE" resultMap="UserResultMap">
        { CALL up_user_find_by_provider(#{provider}, #{providerId}) }
    </select>

    <select id="findById" statementType="CALLABLE" resultMap="UserResultMap">
        { CALL up_user_find_by_id(#{userId}) }
    </select>

    <!-- 최초 가입이면 INSERT, 이미 있으면 닉네임·이메일·프로필 이미지·updated_at 갱신 -->
    <update id="upsertUser" statementType="CALLABLE">
        { CALL up_user_upsert(
            #{provider},
            #{providerId},
            #{providerEmail},
            #{nickname},
            #{profileImage}
        )}
    </update>

</mapper>
